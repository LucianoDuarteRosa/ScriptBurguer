@model HomeViewModel

<partial name = "_Carousel"/>

<h2>Lanches em promoção</h2>
<h4>Tá com fome?</h4>

<div class="row">
    @foreach(var lanche in Model.LancheEmPromocao)
    {
        <partial name="_LanchesResumo" for="@lanche"/>
    }

</div>